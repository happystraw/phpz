name: macOS Test

on:
  push:
    branches: ["dev"]
    paths:
      - "src/**"
      - "examples/**"
      - "build.zig"
      - "build.zig.zon"
      - ".github/workflows/test-macos.yml"
  pull_request:
    branches: ["dev"]
    paths:
      - "src/**"
      - "examples/**"
      - "build.zig"
      - "build.zig.zon"
      - ".github/workflows/test-macos.yml"
  workflow_dispatch:

jobs:
  test:
    name: test (${{ matrix.php }}, ${{ matrix.arch }}, ${{ matrix.debug && 'debug' || 'release' }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        php: ["8.2", "8.3", "8.4", "8.5"]
        arch: [x86_64, aarch64]
        debug: [true, false]
        include:
          - arch: x86_64
            os: macos-15-intel
          - arch: aarch64
            os: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - uses: mlugg/setup-zig@v2
      - name: Setup PHP (${{ matrix.debug && 'debug' || 'release' }})
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          tools: php-config
        env:
          debug: ${{ matrix.debug }}
      - name: Run unit tests
        run: |
          PHP_INCLUDE_DIR=$(php-config --include-dir)
          zig build test -Dphp-include-dir="$PHP_INCLUDE_DIR"
      - name: Run example tests
        run: |
          PHP_INCLUDE_DIR=$(php-config --include-dir)
          zig build test-examples -Dphp-include-dir="$PHP_INCLUDE_DIR"
      - name: Run example tests (ReleaseSafe)
        run: |
          PHP_INCLUDE_DIR=$(php-config --include-dir)
          zig build test-examples -Dphp-include-dir="$PHP_INCLUDE_DIR" -Doptimize=ReleaseSafe
      - name: Run example tests (ReleaseFast)
        run: |
          PHP_INCLUDE_DIR=$(php-config --include-dir)
          zig build test-examples -Dphp-include-dir="$PHP_INCLUDE_DIR" -Doptimize=ReleaseFast
