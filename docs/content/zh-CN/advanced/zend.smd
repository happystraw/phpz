---
.title = "底层 Zend / PHP C API",
.date = .date("2026-09-16T00:00:00"),
.layout = "doc.shtml",
.translation_key = "advanced/zend",
.custom = { "section": "进阶主题" },
---

`phpz.c` 暴露项目头文件翻译出的 C 绑定；`phpz.zend` 提供常用结构包装。高层接口没有覆盖的能力可以从这里进入。

# [包装与原始指针]($heading.id('wrappers-and-raw-pointers'))

```zig
const raw: *phpz.c.zval = ctx.retval.ptr();
const wrapped = phpz.Zval.from(raw);
wrapped.set(.int, 42);
```

包装转换不分配新值、不增加引用计数。要跨作用域保存值，仍需按具体值的所有权规则处理，不能只保存栈上 zval 的地址。

# [从源码确定调用方式]($heading.id('check-source-contracts'))

1. 确定当前 PHP 版本和构建配置。
2. 阅读对应 `Zend/` 或 `main/` 头文件的签名。
3. 查实现和调用方，确认借用、转移、异常与 bailout 行为。
4. 检查 phpz 是否已有包装，避免重复实现。
5. 用真实 PHP 加载扩展运行最小调用验证。

C 宏、可变参数和调用约定不能只凭函数名推断。需要 C 边界的宏或控制流，参考 `build/phpz_wrapper.c` 与 `.h` 的实现方式。

# [典型风险]($heading.id('common-risks'))

| 情况 | 需要确认的事项 |
| --- | --- |
| 返回 `zend_object*` | 新拥有引用还是借用对象 |
| 操作 zval | 旧值是否先析构，是否保留引用包装 |
| 调用用户代码 | PHP 异常和 bailout 是否可能发生 |
| 保存引擎指针 | 是否受请求、线程或表变更影响 |
| 跨平台链接 | PHP ABI、ZTS 和 debug 配置是否匹配 |

源码链接用于定位，行为结论应绑定到实际使用的 PHP 与 phpz 版本。

# [源码与示例]($heading.id('source-and-examples'))

- [src/c.zig](https://github.com/happystraw/phpz/blob/dev/src/c.zig)
- [src/zend.zig](https://github.com/happystraw/phpz/blob/dev/src/zend.zig)
- [build/phpz_wrapper.h](https://github.com/happystraw/phpz/blob/dev/build/phpz_wrapper.h)
- [build/phpz_wrapper.c](https://github.com/happystraw/phpz/blob/dev/build/phpz_wrapper.c)
- [src/abi.zig](https://github.com/happystraw/phpz/blob/dev/src/abi.zig)
